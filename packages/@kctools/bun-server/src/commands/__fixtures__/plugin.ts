/**
 * Minimal bundler plugin fixture used by build command tests.
 * The build action imports the module a bunfig plugin name resolves to and
 * reads its default export.
 */
export default { name: "fixture-plugin" };
